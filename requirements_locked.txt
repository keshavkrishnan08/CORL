# DRC CoRL 2026 — dependency notes.
#
# DO NOT `pip install -r requirements_locked.txt` on Kaggle for the sim stack:
#  - `libero` is NOT a usable PyPI package (it installs from GitHub source), and
#  - the legacy pinned versions fight Kaggle's modern base (Python 3.12 / numpy 2 / torch 2.10).
# Use `bash scripts/kaggle_setup.sh` instead, which installs LIBERO from source and a
# compatible Robomimic/Robosuite/MuJoCo line, ending with an import check.
#
# --- Pure-analysis / dev box (CPU, no simulator): these DO pip-install cleanly ---
numpy<2
scipy
pandas
scikit-learn
statsmodels
matplotlib
PyYAML
torch
# --- Simulation stack: install via scripts/kaggle_setup.sh (from source), not pinned here ---
#   LIBERO        (git clone https://github.com/Lifelong-Robot-Learning/LIBERO ; pip install -e .)
#   robomimic     (pip install robomimic)
#   robosuite     (pip install "robosuite>=1.4")
#   mujoco        (pip install "mujoco>=3.1")
#   h5py, opencv-python-headless
